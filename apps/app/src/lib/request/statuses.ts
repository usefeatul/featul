/** Statuses shown by default in workspace request lists. Settled requests remain available through filters. */
export const DEFAULT_REQUEST_STATUSES = [
  "pending",
  "review",
  "planned",
  "progress",
] as const
